package main

import (
	"bufio"
	"fmt"
	"os"
	"strconv"
)

func changeBit(number int64, bit int){
	number
}
func main() {
	var number int64
	fmt.Println("Please enter number:")
	scanner := bufio.NewScanner(os.Stdout)
	for scanner.Scan{
		scanner.Text()
		bit, _ := strconv.Atoi(scanner.Text())
	}
	fmt.Println(number)
}

channel := make(chan int)

scanner.Scan()
